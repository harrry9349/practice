go run primenumber.go  -low 10
引数が入力されていません
0.000995秒
go run primenumber.go  -high 10
引数が入力されていません
0.000998秒
go run primenumber.go -low 2 -high 10
2は素数です。
3は素数です。
4は素数ではありません。
5は素数です。
6は素数ではありません。
7は素数です。
8は素数ではありません。
9は素数ではありません。
10は素数ではありません。
0.001027秒
go run primenumber.go -low 100 -high 10
10は素数ではありません。
11は素数です。
12は素数ではありません。
13は素数です。
14は素数ではありません。
15は素数ではありません。
16は素数ではありません。
17は素数です。
18は素数ではありません。
19は素数です。
20は素数ではありません。
21は素数ではありません。
22は素数ではありません。
23は素数です。
24は素数ではありません。
25は素数ではありません。
26は素数ではありません。
27は素数ではありません。
28は素数ではありません。
29は素数です。
30は素数ではありません。
31は素数です。
32は素数ではありません。
33は素数ではありません。
34は素数ではありません。
35は素数ではありません。
36は素数ではありません。
37は素数です。
38は素数ではありません。
39は素数ではありません。
40は素数ではありません。
41は素数です。
42は素数ではありません。
43は素数です。
44は素数ではありません。
45は素数ではありません。
46は素数ではありません。
47は素数です。
48は素数ではありません。
49は素数ではありません。
50は素数ではありません。
51は素数ではありません。
52は素数ではありません。
53は素数です。
54は素数ではありません。
55は素数ではありません。
56は素数ではありません。
57は素数ではありません。
58は素数ではありません。
59は素数です。
60は素数ではありません。
61は素数です。
62は素数ではありません。
63は素数ではありません。
64は素数ではありません。
65は素数ではありません。
66は素数ではありません。
67は素数です。
68は素数ではありません。
69は素数ではありません。
70は素数ではありません。
71は素数です。
72は素数ではありません。
73は素数です。
74は素数ではありません。
75は素数ではありません。
76は素数ではありません。
77は素数ではありません。
78は素数ではありません。
79は素数です。
80は素数ではありません。
81は素数ではありません。
82は素数ではありません。
83は素数です。
84は素数ではありません。
85は素数ではありません。
86は素数ではありません。
87は素数ではありません。
88は素数ではありません。
89は素数です。
90は素数ではありません。
91は素数ではありません。
92は素数ではありません。
93は素数ではありません。
94は素数ではありません。
95は素数ではありません。
96は素数ではありません。
97は素数です。
98は素数ではありません。
99は素数ではありません。
100は素数ではありません。
0.002960秒
